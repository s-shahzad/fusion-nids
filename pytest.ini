[pytest]
testpaths = tests
markers =
    integration: multi-component integration test
    slow: long-running test
    live: requires live packet capture or a network interface
    environment: requires specific environment tooling
    lab: requires the self-hosted NIDS lab runner
